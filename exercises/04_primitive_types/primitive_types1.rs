// Булеві значення (Booleans) (`bool`)

fn main() {
    let is_morning = true;
    if is_morning {
        println!("Доброго ранку!");
    }

    // TODO: Визначте булеву змінну з назвою `is_evening` перед наступним оператором `if`.
    // Значення змінної повинно бути запереченням (протилежним) до `is_morning`.
    // let …
    if is_evening {
        println!("Доброго вечора!");
    }
}
