fn main() {
    // У Rust змінні за замовчуванням є незмінюваними (immutable).
   // Додавання ключового слова `mut` після `let` робить оголошену змінну змінюваною (mutable).
    let mut x = 3;
    println!("Число {x}");

    x = 5;
    println!("Число {x}");
}
