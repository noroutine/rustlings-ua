fn square(num: i32) -> i32 {
    // Приберіть крапку з комою `;` в кінці останнього рядка щоб неявно повернути з функції результат виразу.
    num * num
}

fn main() {
    let answer = square(3);
    println!("3 в квадраті буде {answer}");
}
